# Cloudflare R2 Configuration
# These credentials are from: Cloudflare Dashboard > R2 > Manage R2 API Tokens

# R2 API Access Key ID
R2_ACCESS_KEY_ID=

# R2 API Secret Access Key
R2_SECRET_ACCESS_KEY=

# R2 Bucket Name (must match the bucket you created)
R2_BUCKET_NAME=notion-images

# Cloudflare Account ID (found in Dashboard URL or Overview page)
R2_ACCOUNT_ID=

# Workers Proxy Configuration
# Set these after deploying the Cloudflare Worker

# The full URL of your deployed Worker
# Example: https://notion-r2-image-proxy.your-subdomain.workers.dev
WORKERS_PROXY_URL=

# Authentication token for the Worker proxy
# Generate with: openssl rand -hex 32
# This must match the AUTH_TOKEN secret in your Worker
WORKERS_AUTH_TOKEN=
